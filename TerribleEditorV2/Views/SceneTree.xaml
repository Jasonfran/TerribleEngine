<UserControl x:Class="TerribleEditorV2.Views.SceneTree"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TerribleEditorV2.Views"
             xmlns:models="clr-namespace:TerribleEditorV2.Models"
             xmlns:controller="clr-namespace:TerribleEditorV2.Controller"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <TreeView ItemsSource="{Binding Entities}">
            <TreeView.Resources>
                <HierarchicalDataTemplate DataType="{x:Type models:SceneTreeViewModel}" ItemsSource="{Binding Entities}">
                    <Label Content="{Binding Id}" />
                </HierarchicalDataTemplate>

                <HierarchicalDataTemplate DataType="{x:Type models:EntityNodeViewModel}" ItemsSource="{Binding Children}">
                    <Label Content="{Binding Id}" />
                </HierarchicalDataTemplate>
            </TreeView.Resources>
        </TreeView>
    </Grid>
</UserControl>
